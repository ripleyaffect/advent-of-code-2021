
/**
 *
 * @param length Number length of desired range
 * @returns Array of length `length`, filled with indices
 */
const range = (length: number) => Array.from({ length }, (_, i) => i)

export default range
